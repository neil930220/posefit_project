{
  "build_command": "cd backend && pip install -r requirements/production.txt && python manage.py collectstatic --noinput",
  "start_command": "cd backend && gunicorn config.wsgi:application --bind 0.0.0.0:$PORT",
  "python_version": "3.12"
}

